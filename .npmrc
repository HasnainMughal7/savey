# @clerk/expo-passkeys supports Expo 54 at runtime but its published React peer range skips the
# exact React 19.1.0 version pinned by Expo SDK 54. Keep npm installs reproducible until upstream
# broadens that peer range.
legacy-peer-deps=true
